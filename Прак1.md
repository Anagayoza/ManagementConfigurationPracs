1.  ```
grep -o "^[^:]*" passwd | sort
```

2.  ```
grep -v '^#' /etc/protocols | awk '{print $2, $1}' | sort -rn | head -n 5
   ```

3. ```
#!/bin/bash
text="$1"
len=${#text}`
dashes=$(printf '%*s' $((len + 2)) '' | tr ' ' '-')
 
echo "+$dashes+"
echo "| $text |"
echo "+$dashes+"
```

4. ```
sed 's/"[^"]*"//g' hello.c | grep -oE '[a-zA-Z_][a-zA-Z0-9_]*' | sort -u | tr '\n' ' '
```

5. ```
#!/bin/bash
if [ ! -f "$1" ]; then
	echo "Couldn't find the file"
	exit 1
fi

chmod 755 "$1"
sudo cp "$1" usr/local/bin/
echo "Done"
```

6. ```
#!/bin/bash
find . -type f \( -name "*.c" -o -name "*.js" -o -name "*.py" \) | while read -r file; do
    first_line=$(head -n 1 "$file")

    if [[ "$first_line" =~ ^[[:space:]]*(#|//|/\*) ]]; then
        echo "true, $file has comment in first line"
    else
        echo "false, $file hasn't comment in first line"
    fi
done
   ```

8. ```
#!/bin/bash

sed 's/    /\t/g' "$1" > "$2"
echo "Done"
```